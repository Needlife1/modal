const arrayOfCategories = categories.querySelector('item');
console.log(`Number of categories: ${arrayOfCategories.length}`);

const arrayOfHeader = categories.querySelector('h2');
console.log(arrayOfCategories);
